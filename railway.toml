[build]
builder = "NIXPACKS"
buildCommand = "npm run install:all && npm run build && cp -r frontend/build backend/public"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "TaskNest-api"
